#include "main.h"
#include <stdio.h>

/**
 * mul- function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return:0 success, non-zero digit if error
 */

int mul(int a, int b)
{
	int x;

	x = a * b;

	return (x);
}
