#include "main.h"

/**
 * add- Adds two integers and returns the result
 * @x: number being tested
 * @y: number being tested
 * Return:0 success, non-ero digit if error
 */

int add(int x, int y)
{
	return (x + y);
}
