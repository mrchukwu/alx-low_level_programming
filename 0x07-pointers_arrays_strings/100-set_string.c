/**
 * set_string- function that sets the valuse of a pointer to a char
 * @s: pointer to the address of string s
 * @to: string to set value to;
 * Return: nothing
 */

void set_string(char **s, char *to)
{
	*s = to;
}
