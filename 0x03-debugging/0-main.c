#include "main.h"

/**
 * main- test function that prints if the integer is positive or negative
 * Return:0 success, non-zero digit if error
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
